pipeline {
	 agent any
	 stages {		
    		stage('Installing packages') {
            		steps {
                		script {
                    			sh 'pip -r requirements.txt'
                		}
         		}
     		}
	}
}  
